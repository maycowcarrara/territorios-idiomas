{
  "name": "notifications-relay",
  "private": true,
  "scripts": {
    "deploy": "node ../../scripts/check-deploy-auth.cjs wrangler idiomas --config wrangler.idiomas.toml --cwd . && wrangler deploy --config wrangler.idiomas.toml",
    "deploy:idiomas": "npm run deploy",
    "dev": "wrangler dev --config wrangler.idiomas.toml",
    "dev:idiomas": "npm run dev",
    "tail": "wrangler tail --config wrangler.idiomas.toml",
    "tail:idiomas": "npm run tail"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
